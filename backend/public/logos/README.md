# Logo Files

Place logo images here:

- `logo-light.png` - Logo for light theme (white background)
- `logo-dark.png` - Logo for dark theme (dark background)

The logos will be served via `/api/logo/:theme` endpoint.

