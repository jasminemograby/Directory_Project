# איך למצוא את ה-Backend Terminal עם ה-Logs

## הבעיה
אם אתה רואה שגיאה `Error: listen EADDRINUSE: address already in use :::5000`, זה אומר שהשרת כבר רץ בטרמינל אחר.

## פתרון: מצא את הטרמינל הנכון

### דרך 1: חפש את הטרמינל שבו השרת רץ

1. **פתח את כל חלונות הטרמינל/PowerShell שלך**
2. **חפש את הטרמינל שבו כתוב:**
   ```
   Server running on port 5000
   Database connected successfully
   ```
   או
   ```
   Listening on port 5000
   ```

3. **זה הטרמינל הנכון!** - שם תראה את כל ה-Logs

### דרך 2: אם לא מוצא את הטרמינל

1. **סגור את כל הטרמינלים**
2. **פתח טרמינל חדש והרץ:**
   ```powershell
   cd backend
   npm start
   ```
3. **עכשיו תראה את כל ה-Logs בטרמינל הזה**

### דרך 3: עצור את השרת הישן והפעל מחדש

אם אתה רוצה לעצור את השרת הישן:

1. **מצא את הטרמינל שבו השרת רץ**
2. **לחץ `Ctrl+C` כדי לעצור אותו**
3. **הרץ מחדש:**
   ```powershell
   cd backend
   npm start
   ```

## מה תראה ב-Logs כשאתה שולח טופס

כשאתה שולח את הטופס, תראה בטרמינל:

```
═══════════════════════════════════════════════════════
📝 Step4 Registration Request
═══════════════════════════════════════════════════════
Registration ID: ...
Employees Count: 1
Departments Count: 0
Learning Path Policy: auto
Has Decision Maker: false
Employees:
  1. Employee Name (email@example.com)
═══════════════════════════════════════════════════════

📁 Processing 0 departments...

👥 Processing 1 employees...

   Processing employee: Employee Name (email@example.com)
✅ Employee processed: Employee Name (email@example.com) - ID: ...
```

אם יש שגיאת validation, תראה:

```
Validation errors: [
  {
    type: 'field',
    value: [],
    msg: 'At least one department is required',
    path: 'departments',
    location: 'body'
  }
]
Request body: {
  "registrationId": "...",
  "employees": [...],
  "departments": [],
  ...
}
```

## טיפים

- **שמור את הטרמינל פתוח** - כך תראה את כל ה-Logs בזמן אמת
- **גלול למעלה** - אם יש הרבה Logs, גלול למעלה כדי לראות את השגיאות
- **העתק את ה-Logs** - אם אתה צריך עזרה, העתק את כל ה-Logs ושלח לי

